# Interní materiály, které nepatří do veřejného repozitáře.
#
# Pozn.: dřívější verze tohoto souboru skrývala i pracovní specifikace a zadání
# (SPEC*.md, ZADANI-*.md, PREDANI-*.md). Ty byly 1. 8. 2026 vráceny do repozitáře
# na pokyn uživatele — jsou to živé dokumenty, na kterých stojí předávání práce
# mezi sessions, a mimo git se ztrácely.
#
# Historie byla 1. 8. 2026 přepsána přes git-filter-repo, takže starší kopie
# těchto souborů v ní nejsou. Nově přidané verze v historii budou.

# Značkový manuál — velký binární soubor od třetí strany, do gitu nepatří
Alba_logomanual.pdf

# Nasazovací návod — může obsahovat údaje o infrastruktuře
DEPLOY.md

# Lokalni nastaveni stroje, ne projektu
.claude/settings.local.json
